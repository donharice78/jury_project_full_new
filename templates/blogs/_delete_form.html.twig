<form method="post" action="{{ path('app_blogs_delete', {'id': blog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id) }}">
    <button class="btn btn-danger btn-lg mb-3">Delete Blog</button>
</form>
